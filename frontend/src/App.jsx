// Versão ativa: v1.0.1 — lista de e-mails por cliente, histórico de envios, empresa faturante; parcelas com coluna separada; sem C.Custo nas tabelas
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v1.0.1.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
