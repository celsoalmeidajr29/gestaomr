// Versão ativa: v51 — consolida Faturas+Fechamentos + envio de medição por email (PDF + XLSX)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v51.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
