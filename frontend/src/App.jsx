// Versão ativa: v88 — Indicadores "Sem PIX" (Funcionários + Folha) + status workflow pendente/transferido/pago/cancelada
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v88.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
