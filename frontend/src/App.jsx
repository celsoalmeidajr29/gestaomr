// Versão ativa: v1.0.11 — PDF proposta: logos proporcionais + MR Tracker + prestador em escopo + badge EVENTOS
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v1.0.11.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
