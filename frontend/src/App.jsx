// Versão ativa: v71 — XML NF-e: valor sem retenções + editar competência da fatura
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v71.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
