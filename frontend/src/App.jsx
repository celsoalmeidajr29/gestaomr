// Versão ativa: v56 — UX da competência: segue a data por padrão, só persiste override quando explícito
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v56.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
