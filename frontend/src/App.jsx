// Versão ativa: v1.0.12 — PDF proposta: compressão JPEG nas imagens (PDF mais leve)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v1.0.12.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
