// Versão ativa: v89 — Botão "Transferir via Cora" na Folha + modal + chamada backend (F2)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v89.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
