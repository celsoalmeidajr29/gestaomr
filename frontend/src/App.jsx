// Versão ativa: v87 — PDF do Resumo + Dashboard com filtro de mês + Folha status pendente/paga/cancelada + Faturas XML mostram lançamento sintético
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v87.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
