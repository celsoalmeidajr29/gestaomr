// Versão ativa: v79 — Resumo folha por categoria usa folhasPorFunc (alinha com aba Folha de Pagamento)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v79.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
