// Versão ativa: v62 — Remove todos os cálculos e visualizações de salário fixo (folha, resumo, PDFs, modais)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v62.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
