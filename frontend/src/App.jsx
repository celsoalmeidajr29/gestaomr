// Versão ativa: v96 — Parcelas: criação parcelada (despesas/chefia/vales) + botão one-shot de migração das parcelas existentes
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v96.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
