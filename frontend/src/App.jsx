// Versão ativa: v66 — Fix responsividade em altas resoluções (wrapper centralizado)
// Versões anteriores em src/versions/
import MRSysApp from './versions/MRSys_v66.jsx'

export default function App(props) {
  return <MRSysApp {...props} />
}
