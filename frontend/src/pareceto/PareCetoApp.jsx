// Versão ativa: v0.1.2 — fix HTTP 500 (padrão 30 dias + seletor de período + LIMIT 30k)
// Versões anteriores em pareceto/versions/
import PareCetoApp from './versions/PareCetoApp_v0.1.2.jsx'
export default PareCetoApp
