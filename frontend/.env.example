# ============================================================================
# MRSys Frontend — Variáveis de Ambiente (template)
# ============================================================================
# Copie para .env.local em desenvolvimento.
# Em produção, configure no painel do Cloudflare Pages
# (Settings → Environment variables → Production).
# ============================================================================

# URL base da API (backend PHP).
#
# Desenvolvimento local (frontend e backend na mesma origem via proxy do Vite):
#   VITE_API_BASE=/api
#
# Produção híbrida (frontend no Cloudflare Pages, backend no cPanel):
#   VITE_API_BASE=https://mrsys.grupomr.seg.br/api
#
# Importante: o domínio aqui deve estar listado em CORS_ALLOWED_ORIGINS no
# .env do backend. Caso contrário o navegador bloqueia as chamadas.
VITE_API_BASE=https://mrsys.grupomr.seg.br/api
